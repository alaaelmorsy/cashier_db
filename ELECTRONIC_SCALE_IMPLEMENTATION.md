# آلية عمل الميزان الإلكتروني في نظام الكاشير
## Electronic Scale Barcode Implementation Guide

---

## 📋 نظرة عامة | Overview

هذا المستند يشرح بالتفصيل آلية التعامل مع الباركود الصادر من الميزان الإلكتروني في أنظمة نقاط البيع (POS).
الميزان الإلكتروني يصدر باركود خاص يحتوي على معلومات الصنف والوزن/السعر معاً.

---

## 🔢 تنسيق الباركود | Barcode Format

### البنية الأساسية للباركود (13 رقم)

```
┌─────┬──────────┬──────────┬───┐
│ PP  │  IIIII   │  VVVVV   │ C │
└─────┴──────────┴──────────┴───┘
  2       5           5        1  = 13 digits
```

**شرح الأجزاء:**

| الموضع | الطول | الوصف | مثال |
|--------|-------|-------|------|
| `PP` | 2 | **Prefix**: البادئة (عادة 20 أو 21 أو 22) | `20` |
| `IIIII` | 5 | **Item Code**: كود الصنف | `12345` |
| `VVVVV` | 5 | **Value**: الوزن أو السعر | `00350` |
| `C` | 1 | **Checksum**: رقم التحقق (اختياري) | `0` |

### مثال باركود كامل:
```
2012345003500
││└───┘└───┘└─ Checksum
││  │    └───── Weight/Price: 00350
││  └────────── Item Code: 12345
│└───────────── Prefix: 20
```

---

## ⚙️ آلية العمل | Working Mechanism

### الخوارزمية الأساسية

```
START
  ↓
تلقي الباركود من الماسح الضوئي
  ↓
هل طول الباركود = 13؟
  ├─ لا → بحث عادي بالباركود الكامل
  └─ نعم → معالجة باركود الميزان
       ↓
    استخراج كود الصنف (المواضع 2-7)
       ↓
    البحث عن الصنف في قاعدة البيانات
       ↓
    هل الصنف موجود؟
      ├─ لا → رسالة خطأ: صنف غير موجود
      └─ نعم → تحديد نوع الميزان
            ↓
        هل نوع الميزان = وزن؟
          ├─ نعم → استخراج الوزن (المواضع 7-12)
          │         الوزن بالكيلو = القيمة ÷ 1000
          │         إضافة الصنف بالوزن المحدد
          │
          └─ لا (سعر) → استخراج السعر (المواضع 7-12)
                        الريالات = المواضع 7-10
                        الهللات = المواضع 10-12 ÷ 100
                        السعر الكلي = الريالات + الهللات
                        إضافة الصنف بالسعر المحدد
END
```

---

## 💻 الكود المرجعي | Reference Code

### 1. دالة معالجة الباركود الرئيسية

```java
public void processBarcodeInput(String barcode, Customer customer) {
    // البحث الأولي بالباركود الكامل
    Product product = productRepository.findByBarcode(barcode);
    
    if (product != null) {
        // صنف عادي - إضافة مباشرة
        addProductToCart(product, customer, 1.0, 0.0);
    } 
    else if (barcode != null && barcode.length() == 13) {
        // محتمل أن يكون باركود ميزان
        processElectronicScaleBarcode(barcode, customer);
    } 
    else {
        // باركود غير موجود
        showError("المنتج غير موجود");
    }
}
```

### 2. دالة معالجة باركود الميزان

```java
private void processElectronicScaleBarcode(String barcode, Customer customer) {
    // استخراج كود الصنف (من الموضع 2 إلى 7)
    String productCode = barcode.substring(2, 7);
    
    // البحث عن الصنف بالكود المستخرج
    Product product = productRepository.findByBarcode(productCode);
    
    if (product != null) {
        // تحديد نوع الميزان من الإعدادات
        boolean isPriceScale = systemSettings.isElectronicScaleType();
        
        if (!isPriceScale) {
            // ميزان الوزن
            processWeightScale(barcode, product, customer);
        } else {
            // ميزان السعر
            processPriceScale(barcode, product, customer);
        }
    } else {
        showError("كود الصنف غير موجود: " + productCode);
    }
}
```

### 3. معالجة ميزان الوزن

```java
private void processWeightScale(String barcode, Product product, Customer customer) {
    // استخراج الوزن من المواضع 7-12
    String weightStr = barcode.substring(7, 12);
    
    // تحويل إلى وزن بالكيلو
    // مثال: "00350" = 350 جرام = 0.350 كيلو
    double weightInKg = Long.parseLong(weightStr) / 1000.0;
    
    // إضافة الصنف للسلة بالوزن المحدد
    addProductToCart(product, customer, weightInKg, 0.0);
    
    System.out.println("تم إضافة: " + product.getName() + 
                       " - الوزن: " + weightInKg + " كجم");
}
```

### 4. معالجة ميزان السعر

```java
private void processPriceScale(String barcode, Product product, Customer customer) {
    // استخراج السعر من المواضع 7-12
    // مثال: "12345" = 123.45 ريال
    
    // الريالات (المواضع 7-10)
    String riyalStr = barcode.substring(7, 10);
    double riyals = Long.parseLong(riyalStr);
    
    // الهللات (المواضع 10-12)
    String halalaStr = barcode.substring(10, 12);
    double halalas = Long.parseLong(halalaStr) / 100.0;
    
    // السعر الإجمالي
    double totalPrice = riyals + halalas;
    
    // حساب الكمية بناءً على السعر
    // الكمية = السعر المقروء ÷ سعر الوحدة
    double quantity = totalPrice / product.getPrice();
    
    // إضافة الصنف للسلة
    addProductToCart(product, customer, quantity, totalPrice);
    
    System.out.println("تم إضافة: " + product.getName() + 
                       " - السعر: " + totalPrice + " ريال");
}
```

### 5. دالة إضافة الصنف للسلة

```java
private void addProductToCart(Product product, Customer customer, 
                               double quantity, double customPrice) {
    CartItem item = new CartItem();
    item.setProduct(product);
    item.setQuantity(quantity);
    
    // إذا كان هناك سعر مخصص (من ميزان السعر)
    if (customPrice > 0) {
        item.setPrice(customPrice);
    } else {
        // احسب السعر بناءً على الكمية
        item.setPrice(product.getPrice() * quantity);
    }
    
    // إضافة للسلة
    cart.addItem(item);
    
    // إعادة حساب الإجمالي
    recalculateTotal();
}
```

---

## 📊 أمثلة عملية | Practical Examples

### مثال 1: ميزان الوزن - لحم أحمر

**البيانات:**
- اسم الصنف: لحم أحمر
- كود الصنف في النظام: `12345`
- سعر الكيلو: 50 ريال
- الوزن الفعلي: 1.250 كجم

**من الميزان:**
```
الباركود المُصدر: 2012345012500
```

**التحليل:**
```
Prefix:     20
Item Code:  12345
Weight:     01250 → 1250 ÷ 1000 = 1.250 kg
Checksum:   0
```

**النتيجة في الفاتورة:**
```
الصنف: لحم أحمر
الكمية: 1.250 كجم
سعر الكيلو: 50.00 ريال
الإجمالي: 62.50 ريال
```

---

### مثال 2: ميزان الوزن - دجاج

**البيانات:**
- اسم الصنف: دجاج طازج
- كود الصنف: `00567`
- سعر الكيلو: 25 ريال
- الوزن: 0.850 كجم

**من الميزان:**
```
الباركود: 2100567008500
```

**التحليل:**
```
Prefix:     21
Item Code:  00567
Weight:     00850 → 850 ÷ 1000 = 0.850 kg
```

**النتيجة:**
```
الصنف: دجاج طازج
الكمية: 0.850 كجم
السعر: 21.25 ريال
```

---

### مثال 3: ميزان السعر - جبنة

**البيانات:**
- اسم الصنف: جبنة بيضاء
- كود الصنف: `78901`
- الوزن: مختلف
- السعر المحسوب على الميزان: 15.75 ريال

**من الميزان:**
```
الباركود: 2078901157500
```

**التحليل:**
```
Prefix:     20
Item Code:  78901
Price:      15750
  - Riyals:   157 (المواضع 7-10)
  - Halalas:  50 (المواضع 10-12)
  - Total:    157 + 0.50 = 15.75 ريال
```

**النتيجة:**
```
الصنف: جبنة بيضاء
السعر: 15.75 ريال
```

---

## 🗄️ متطلبات قاعدة البيانات | Database Requirements

### 1. جدول الأصناف (Products/Items)

```sql
CREATE TABLE products (
    product_id INT PRIMARY KEY AUTO_INCREMENT,
    product_name VARCHAR(255) NOT NULL,
    barcode VARCHAR(50) NOT NULL,           -- كود الصنف (5 أرقام للميزان)
    price DECIMAL(10,3) NOT NULL,
    is_weight_item BOOLEAN DEFAULT FALSE,   -- هل يُباع بالوزن؟
    -- ... أعمدة أخرى
    INDEX idx_barcode (barcode)
);
```

**مثال البيانات:**
```sql
INSERT INTO products (product_name, barcode, price, is_weight_item) VALUES
('لحم أحمر', '12345', 50.000, TRUE),
('دجاج طازج', '00567', 25.000, TRUE),
('جبنة بيضاء', '78901', 30.000, TRUE);
```

---

### 2. جدول إعدادات النظام (System Settings)

```sql
CREATE TABLE system_settings (
    setting_id INT PRIMARY KEY,
    electronic_scale_enabled BOOLEAN DEFAULT FALSE,
    electronic_scale_type BOOLEAN DEFAULT FALSE,  -- FALSE=وزن, TRUE=سعر
    -- ... إعدادات أخرى
);
```

**القيم:**
```sql
-- تفعيل ميزان الوزن
UPDATE system_settings SET 
    electronic_scale_enabled = TRUE,
    electronic_scale_type = FALSE
WHERE setting_id = 1;

-- OR تفعيل ميزان السعر
UPDATE system_settings SET 
    electronic_scale_enabled = TRUE,
    electronic_scale_type = TRUE
WHERE setting_id = 1;
```

---

## ⚠️ التعامل مع الأخطاء | Error Handling

### الحالات الخاصة

```java
public void processElectronicScaleBarcode(String barcode, Customer customer) {
    // 1. التحقق من طول الباركود
    if (barcode == null || barcode.length() != 13) {
        throw new InvalidBarcodeException("الباركود يجب أن يكون 13 رقم");
    }
    
    // 2. التحقق من أن الباركود أرقام فقط
    if (!barcode.matches("\\d{13}")) {
        throw new InvalidBarcodeException("الباركود يجب أن يحتوي على أرقام فقط");
    }
    
    // 3. استخراج كود الصنف
    String productCode = barcode.substring(2, 7);
    Product product = productRepository.findByBarcode(productCode);
    
    if (product == null) {
        // كود الصنف غير موجود
        showError("كود الصنف " + productCode + " غير مسجل في النظام");
        // عرض نافذة لإضافة صنف جديد (اختياري)
        showAddNewProductDialog(productCode);
        return;
    }
    
    // 4. التحقق من أن الصنف يُباع بالوزن
    if (!product.isWeightItem()) {
        showWarning("الصنف " + product.getName() + " لا يُباع بالوزن");
        return;
    }
    
    // 5. استخراج الوزن/السعر
    try {
        String valueStr = barcode.substring(7, 12);
        long value = Long.parseLong(valueStr);
        
        if (value == 0) {
            showError("الوزن/السعر لا يمكن أن يكون صفر");
            return;
        }
        
        // متابعة المعالجة...
        
    } catch (NumberFormatException e) {
        throw new InvalidBarcodeException("خطأ في قراءة الوزن/السعر");
    }
}
```

---

## 🔧 إعدادات الميزان | Scale Configuration

### إعدادات الميزان الموصى بها

**1. للموازين التي تصدر الوزن:**
```
Prefix: 20 أو 21
Format: PPIIIIIWWWWWC
Weight Unit: Grams (جرام)
Weight Digits: 5
Decimal Position: 3 (xxx.xx)
```

**2. للموازين التي تصدر السعر:**
```
Prefix: 20 أو 22
Format: PPIIIIIPRRRRC
Price Digits: 5
Decimal Position: 2 (xxx.xx)
Currency: Riyal/Halala
```

---

## 🧪 اختبار الميزان | Testing

### سيناريوهات الاختبار

```java
@Test
public void testWeightScaleBarcode() {
    // Arrange
    String barcode = "2012345012500";  // 1.250 kg
    Product product = new Product("12345", "لحم", 50.0);
    
    // Act
    processElectronicScaleBarcode(barcode, customer);
    
    // Assert
    assertEquals(1.250, cart.getLastItem().getQuantity());
    assertEquals(62.50, cart.getLastItem().getTotal());
}

@Test
public void testPriceScaleBarcode() {
    // Arrange
    String barcode = "2078901157500";  // 15.75 SAR
    Product product = new Product("78901", "جبنة", 30.0);
    
    // Act
    processElectronicScaleBarcode(barcode, customer);
    
    // Assert
    assertEquals(15.75, cart.getLastItem().getPrice());
}

@Test
public void testInvalidBarcodeLength() {
    String barcode = "20123450125";  // 11 digits only
    
    assertThrows(InvalidBarcodeException.class, () -> {
        processElectronicScaleBarcode(barcode, customer);
    });
}

@Test
public void testProductNotFound() {
    String barcode = "2099999012500";  // Product 99999 doesn't exist
    
    // Should show error dialog
    processElectronicScaleBarcode(barcode, customer);
    verify(errorDialog).show("كود الصنف 99999 غير مسجل");
}
```

---

## 📱 واجهة المستخدم | User Interface

### نافذة الإعدادات

```
┌─────────────────────────────────────────┐
│  ⚙️  إعدادات الميزان الإلكتروني       │
├─────────────────────────────────────────┤
│                                         │
│  ☑️ تفعيل الميزان الإلكتروني            │
│                                         │
│  نوع الميزان:                           │
│  ⚫ ميزان الوزن (Weight Scale)          │
│  ⚪ ميزان السعر (Price Scale)           │
│                                         │
│  البادئة المسموحة:                      │
│  [20] [21] [22]                         │
│                                         │
│  ☑️ عرض تأكيد عند إضافة صنف بالميزان    │
│  ☑️ صوت تنبيه عند قراءة الميزان         │
│                                         │
│  [ حفظ ]  [ إلغاء ]                     │
└─────────────────────────────────────────┘
```

---

## 🎯 نصائح التنفيذ | Implementation Tips

### 1. التحقق من الباركود
- تأكد من أن الباركود 13 رقم بالضبط
- تحقق من أن البادئة صحيحة (20، 21، 22)
- تحقق من أن كود الصنف موجود في النظام

### 2. معالجة القيم
- قسّم الوزن على 1000 للحصول على الكيلو
- تعامل مع الأسعار بدقة (decimal precision)
- تجنب الأخطاء في التقريب

### 3. تجربة المستخدم
- عرض رسالة واضحة عند إضافة صنف بالميزان
- إمكانية تعديل الوزن/السعر يدوياً
- صوت تنبيه مميز لقراءة الميزان

### 4. الأداء
- استخدم index على عمود الباركود
- Cache للإعدادات المتكررة
- معالجة سريعة للباركود (< 100ms)

---

## 📚 مراجع إضافية | Additional References

### أنواع الموازين الشائعة

| النوع | البادئة | التنسيق | الاستخدام |
|------|---------|---------|-----------|
| CAS LP | 20 | PPIIIIIWWWWWC | محلات اللحوم |
| Toledo | 21 | PPIIIIIWWWWWC | السوبر ماركت |
| DIGI | 22 | PPIIIIIWWWWWC | البقالات |
| Bizerba | 23 | PPIIIIIWWWWWC | الأسواق الكبيرة |

### معايير الباركود

- **EAN-13**: المعيار الأوروبي (الأكثر شيوعاً)
- **UPC-A**: المعيار الأمريكي
- **PLU Codes**: للمنتجات الطازجة

---

## ✅ قائمة التحقق | Checklist

قبل التنفيذ تأكد من:

- [ ] فهم تنسيق باركود الميزان (13 رقم)
- [ ] تحديد نوع الميزان (وزن أو سعر)
- [ ] إعداد جدول الأصناف بباركود 5 أرقام
- [ ] إضافة عمود `electronic_scale_type` للإعدادات
- [ ] تنفيذ دالة استخراج كود الصنف `substring(2, 7)`
- [ ] تنفيذ دالة استخراج الوزن `substring(7, 12) / 1000`
- [ ] تنفيذ دالة استخراج السعر
- [ ] التعامل مع الأخطاء والاستثناءات
- [ ] اختبار السيناريوهات المختلفة
- [ ] توثيق الكود بالتعليقات

---

## 📞 الدعم الفني | Support

عند تنفيذ هذه الآلية في نظام جديد، ركز على:

1. **فهم البنية**: تأكد من فهم تنسيق الباركود المكون من 13 رقم
2. **الاختبار**: اختبر مع باركودات حقيقية من الميزان
3. **التوافق**: تأكد من توافق الميزان مع المعيار EAN-13
4. **الإعدادات**: وفّر واجهة لتغيير نوع الميزان بسهولة

---

**تاريخ الإنشاء:** 2026-02-13  
**الإصدار:** 1.0  
**المصدر:** CashierSystem - FutureSoft

---

## 🔗 روابط سريعة

- [BarcodeSettingsController.java](./src/main/java/com/hosny/laundry/backbean/BarcodeSettingsController.java)
- [TypeStockPricesController.java](./src/main/java/com/hosny/laundry/backbean/TypeStockPricesController.java)
- [LaundryInfo.java](./src/main/java/com/hosny/laundry/entity/LaundryInfo.java)

---

**ملاحظة:** هذا المستند يشرح الآلية الفعلية المستخدمة في نظام CashierSystem. 
يمكن تطبيق نفس المبدأ على أي لغة برمجة أو إطار عمل.
