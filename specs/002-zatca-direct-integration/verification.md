# Verification Log: الربط المباشر مع ZATCA

**آخر تحديث**: 2026-07-14

## التحقق الآلي ✅

- `npx jest __tests__/zatca-direct` — **33/33 ناجحة**:
  - `vault.test.js` (5): تشفير/فك AES-256-GCM، صيغة iv:tag:cipher، مفتاح خاطئ، نص مشوه، غياب المفتاح.
  - `router.test.js` (9): كشف الوضع (legacy/unlinked/direct)، الديناميكية وقت الإرسال (FR-004)، أولوية direct، لا تحول تلقائيًا (FR-003)، تفويض legacy للوسيط حرفيًا، direct للقائمة، unlinked لا يرسل، retryUnsent حسب الوضع.
  - `mapper.test.js` (13): مبسطة/ضريبية، أسعار شاملة/غير شاملة، توزيع الخصم، رسوم إضافية، شبكة أمان الإجمالي (الحالتان)، BR-16، ضريبة 0%، أصناف معفاة، عنوان شركة ناقص، sandbox VAT، إشعار دائن 381 ومرجعه.
  - `queue.test.js` (6): بوابة production_ready، ترتيب المعلق ثم غير المرسل، توقف الدفعة عند الخطأ الشبكي فقط، منع الازدواج، التسلسل.
- `npx jest` (الحزمة الكاملة) — **267/267 ناجحة، 21 suite** — لا كسر لأي اختبار قائم.
- فحص صياغة `node --check` على كل الملفات الجديدة/المعدلة (19 ملفًا) — نظيف.

## التحقق اليدوي (يتطلب تشغيل التطبيق مع MySQL وبيئة الهيئة) ⏳

| السيناريو (quickstart.md) | الحالة |
|---|---|
| 1 — استمرارية الوضع القديم بعد الترقية (T011) | ⏳ بانتظار التنفيذ على قاعدة عميل حقيقية مع وسيط 8080 |
| 2 — ربط جديد كامل على sandbox/simulation (T017) | ⏳ يتطلب OTP من بوابة فاتورة (sandbox يقبل أي رمز) |
| 3 — إرسال مبسطة/ضريبية/إشعار دائن (T025) | ⏳ |
| 4 — الانقطاع وإعادة المحاولة وإعادة التشغيل (T028) | ⏳ |
| 5 — الانتقال الطوعي legacy→direct (T033) | ⏳ (التحذير والتأكيد مبنيان في الشاشة) |
| 6 — تنبيهات الشهادة | ⏳ (المنطق مغطى آليًا في onboardingStatus؛ العرض يدوي) |

**خطوات التشغيل**: `npm start` ثم الإعدادات → «الربط الإلكتروني المباشر (بدون وسيط)».
